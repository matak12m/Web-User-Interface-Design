table = document.getElementById("statisticsTable");

function sortTable(header) {

    if (header == "months") {
        //sort the rows by the months, not by numbers
    } 
    else {
        //sort the rows by numbers

    }

}